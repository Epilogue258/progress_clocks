# 2026-08-29

## 项目整体审查（Server + Web 设计思想梳理）

通读全项目（2304 行 TS：common 3 文件 / server 3 文件 / Web 6 文件），评估架构设计。
版本管理用 jj，提交历史 1 天多，commit message 质量高（都写了决策原因）。

### 架构理解（已确认的设计思想）

- **GitHub 模型**：房间 = 仓库，加入 = pull 覆盖本地，新建 = 建仓 + push；本地数据非权威
- **双密码**：joinPwd（读，可空 = 公开房间）+ gmPwd（写，≥6 位），读写权限分离
- **单写者假设 + 乐观锁兜底**：GM 写玩家读，version 冲突返 409 + 最新状态，客户端整体替换（丢包重做）
- **全量覆盖而非增量**：数据几 KB，无 diff/合并复杂度
- **common/ 为共享纯函数层**：几何（clock-geometry）与导出 SVG 三端复用，服务端 resvg 与前端 canvas 渲染同一算法，保证所见即所得
- **契约宽容解析**：parseState 任意输入不抛错，clamp 非法值，未知键忽略 → 天然向后兼容
- **分离模式**：Web/dist 可脱离 server，`?server=` 指向任意后端

### 实测发现的两个缺陷（已用脚本复现验证）

1. **撤销栈 off-by-one（严重）**
   `Web/src/state.ts` 的 `commit()` 在 mutation **之后**才 `undoStack.push(clone(this.state))`，
   导致栈顶恒等于当前状态。实测（mock localStorage 跑真实 Store 类）：
   - 创建两个钟后 undo 一次 → 钟数仍为 2（应为 1）
   - undo 两次 → 1（应为 0），第三次栈空
   - redo 一次 → 2（应为 1）
   - increment 后 undo，fill 不变（撤销完全失效）
   修复方向：在 mutation 之前压快照，或改成 `commit(prevSnapshot)` 传参。

2. **非 ?readonly 的玩家端不轮询**
   `main.ts:367` 仅 `if (urlReadonly) { pollState(...) }` 才启动轮询。
   玩家打开 GM 分享的 `?room=xxx`（不带 readonly）时，既不轮询也不推送，
   只有 bootstrapPull 拉一次 → 全程静态快照，必须手动刷新。
   README 承诺的"玩家零安装打开浏览器即看"实际需要 `?room=xxx&readonly` 才成立。

### 其他待改进点（未验证，代码审阅得出）

- `state.ts:143` updateClock clamp max 到 [1,10]，而契约 `types.ts:81` parseState clamp 到 [2,10]，
  不一致：本地设 max=1 推送后服务器变 2，刷新页面状态跳变
- 零测试覆盖，同步逻辑（乐观锁/冲突/重试）最容易出错却无保护，撤销 bug 即例证
- 密码在 meta.json 明文存、Bearer 明文传、CORS `*`，威胁模型是信任网络（README 已诚实说明）
- `serveStatic` 的 `startsWith(DIST_DIR)` 缺路径分隔符，理论上可匹配 `distevil` 之类前缀目录，
  但 URL 构造已规范化 `..`，实际难以利用，属纵深防御加固项
- 每次页面启动 bootstrapPull → replaceState → notify → 防抖推送，会把刚拉下的状态原样推回，
  version 白涨（无害但脏）

### 评价结论

设计层面相当成熟：分层干净、契约先行、取舍诚实、决策记录完整（README 决策记录一节尤其好）。
问题集中在"实现没跟上设计"——两个核心功能（撤销、玩家实时同步）都是最后一层代码的疏漏，
而非架构缺陷。建议：先修撤销栈 + 轮询条件，再补 5~10 行的最小测试。

---

## 第二轮：修复上述 bug + 前端优化（用户「咖啡」要求，按单元提交）

用户称呼：咖啡。用 jj 管理，每个逻辑单元单独提交，共 9 个：

1. `916c36bc` 修复撤销栈（mutate(fn) 在 fn 之前压快照）
2. `95079847` 修复玩家端轮询（改为按「是否持有写凭证」决定，去掉 ?readonly 依赖）
3. `d211b473` 修复 max 边界（抽 CLOCK_MIN/MAX_SEGMENTS + clampInt 到 common/types）
4. `bf48ed5f` 统一输入框样式（CSS 改通用选择器 + makeInput() helper）
5. `1f08b827` 拖动排序（clock-order.ts + drag-sort.ts，顺序存本地不进契约）
6. `c2d83852` 颜色自定义（取色器 + hex 文本框）+ fill 数字文本框
7. `fb32f099` 交互强化（键盘闭环 + 顶栏按钮分组 + 快捷键说明弹窗）
8. `f4845c1d` 侧边栏搜索 + 已知房间缓存切换（known-rooms.ts）
9. `eaa484e5` server：请求体超限回 413（原先 destroy 连接，客户端只看到 ECONNRESET）

### 关键设计决策（后续改动要遵守）

- **顺序是视图偏好，不是契约数据**：单独 localStorage 键 `progress-clocks:order`，
  不写进 ClockState。Store.subscribe 回调带 `ChangeKind`（'data' | 'order'），
  main.ts 据此跳过推送——排序不该触发同步。
- **拖动把手而非按住卡片拖**：移动端长按已被「打开设置」占用，抢同一段指针序列必打架；
  把手自带 `touch-action: none`，从它起手不会被浏览器当滚动。
- **输入类控件只在 change 落库，不用 input**：否则拖滑条/调取色器每一帧都压一次撤销栈，
  撤销会被噪声淹没。
- **sortByOrder 的兜底返回 0**（依赖 Array.sort 稳定性）保持传入顺序，
  而非按 id 字符串排——id 是「时间戳+随机后缀」，同毫秒创建的钟按 id 排出来是随机的。
- **deleteClock 后选中态落到相邻钟**而非清空，连续删除/继续用方向键时焦点不丢。
- **只缓存验证通过的 GM 密码**（known-rooms），否则下次切换会被无声当成玩家进只读。

### 验证方式（可复用）

把 Web/src 的纯逻辑模块（state / clock-order / known-rooms）复制到临时目录，
sed 改 import 为带 `.ts` 的相对路径，mock 掉 localStorage，
用 `node --experimental-strip-types` 直接跑断言脚本。本次共 30+ 项断言：
撤销栈、排序（重排/前后移/越界/淘汰）、房间缓存（去重/不同服务器同名/脏数据容错）。
服务端用 node 起临时端口 + fetch 脚本做端到端冒烟（CRUD/契约边界/鉴权/乐观锁/导出/安全）。

### 环境注意事项

- 本机执行环境有 safe-delete 守卫，会拦截 `rmSync`/`rm -rf` 递归删除，
  导致 server 的 DELETE /api/room 在测试时返回 500 —— **不是项目 bug**，真实环境正常。
- Git Bash 下 `taskkill /PID` 要加 `MSYS_NO_PATHCONV=1`，否则 `/PID` 被转义成 `//PID`。
- Write 工具写 `/tmp/x` 会落到 `C:\tmp\x`，而 bash 的 `/tmp` 是 AppData\Local\Temp，两者不同。

### 遗留

README 的「Web 功能」一节未同步（顶栏按钮变了、快捷键变多、新增拖动排序与房间缓存）。
已向用户提出，等他决定是否要我更新。
